/**
 * /onboarding/[product]/age-gate
 * S1-F-03 — Age Gate & Parental Consent
 *
 * This page is a shell. The AgeGateForm component will be built
 * in the S1-F-03 sprint batch and imported here.
 */
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Verify Your Age — AceOS',
};

export default function AgeGatePage() {
  return (
    <div className="text-center text-neutral-400 py-20">
      Age gate — coming in S1-F-03
    </div>
  );
}
